# Open loops — the complete list

The reason things felt "lost" is that this list existed only in your head. Here it
is in one place, with the owner and the next action. Anything not on this list is
either finished or deliberately parked, and **parked is a decision, not a failure** —
the difference is that it is written down.

---

## Closed in this work session

| loop | what happened |
|---|---|
| **The AI repository was local-only** | 16 commits / 28,656 lines — voice front end, agent loop, selective memory, keyed long-context store, trained emotion classifier, dashboard + one-call API — were sitting on this disk while the profile README linked the repo as public. **Pushed** to `sushant-me/beyond-attention`. |
| **Claims that could not be checked** | Every claim now lives in `reputation/evidence.json` with its source, and a script re-checks all of them weekly. 19 claims, 15 verified live, 4 documented on request, 0 failing. |
| **Profile README with no verifiable claim on it** | Replaced with the receipts: merged Google patch, five s2geometry issues, HackingHub #1 with numbers, two papers. All 24 links return 200. |
| **Three repositories red for weeks** | `ghar-ko-sathi-nepal` (red since 2026-08-05), `tradiemate-australia`, `tdd-bdd-final-project` — all root-caused, fixed, and green in real CI. |
| **Notifications** | 32 unread → 0, each one explained rather than dismissed. |
| **The portfolio site was the stalest public surface** | `sushantpoudel2028.com.np` did not mention the merged Google patch, the s2geometry issues, the HackingHub rank or either accepted paper, and it omitted the two years at Atmos SoftTech entirely. Rewritten from the verified record, **deployed and confirmed live** (the page now shows all of them), and it links the reputation repo along with a note that the claims are re-checked weekly. |
| **No published writing** | The strongest story in the record — the patch I closed on myself after AddressSanitizer showed it fixed a different bug — is now a public writeup: `sushant-me/writeups` → [*The crash that wasn't*](https://github.com/sushant-me/writeups/blob/main/2026-09-19-the-crash-that-wasnt.md), linked from the profile. |
| **Hunting for openings by hand** | `job-kit/LIVE-OPENINGS.md`: verified platforms that accept Nepal (OpenTrain AI lists it explicitly, $30–65/hr contractor), plus today's specific leads and what to write for each. |
| **Nothing said what you can be hired *for*** | **https://sushant-me.github.io/hire/** is live — one page, no build step, printable: the three services (agent/LLM security review, memory-safety and fuzz-harness work, evaluation design), what a client receives, how an engagement is scoped, and the receipts. Linked from the profile. |
| **One CV had to serve every application** | `cv/Sushant_Poudel_CV_AISecurity.pdf` — a 2-page AI-security CV with the verified record at the top, built from the same evidence file as everything else. The 4-page version stays for contexts that want the full history. |
| **Applications still had to be written from scratch** | `job-kit/APPLICATIONS-READY.md` — four send-ready drafts (Mercor AI red team, BayOne AI security & governance, BirdsVue agentic AI + security, and a cold email to an AI security team), each under 200 words, with the attachments and the exact evidence to cite. Sending them needs your accounts; everything up to that is done. |
| **A client had no way to see the deliverable** | `sushant-me/agent-review-sample` — a deliberately vulnerable 180-line agent and the four-finding review I would return for it: severity, mechanism at file and line, reproduction, fix, and a *"what was not tested"* section. `python3 demo.py` reproduces all four and exits non-zero if one stops reproducing; CI runs it on Python 3.11, 3.12 and 3.13. Linked from the hire page under the review service. |
| **The paper measured a problem and shipped no fix** | `sushant-me/policygate` — a fail-closed policy gate for agent tool calls that encodes the measurement: a model may deny or escalate and **cannot authorise** an uncovered action; a failing or unparseable evaluator falls through to a refusal; every decision lands in a hash-chained audit log. 58 tests, mutation-checked (weakening the model-allow invariant fails three of them), stdlib only, CI green on 3.11–3.13. Live on the hire page and covered by the evidence script. |
| **policygate could not be dropped into anything** | Adapters for MCP-style servers, LangChain-style tools and provider-shaped function calls, all duck-typed so no framework becomes a dependency. `GatedMCPServer` also refuses a server whose tool names collide with names the framework reserves — before any call happens. `examples/agent_integration.py` prints the evidence: four attempts, two reach the server, an offline evaluator executes nothing. **A new adapter found a hole in itself**: an unparseable call was passed through as an ordinary argument, and a name-only allow rule approved arguments the gate never read; it is now escalated without consulting the policy. 58 tests; disabling the model-allow invariant fails three of them, disabling that fix fails three, and disabling both fails six. |
| **The MCP work had no client-facing product** | `sushant-me/mcpaudit` — audit an MCP server's tool declarations before you connect, and pin them so a later change is a diff instead of a silent steering of the agent. Eleven checks across four severities, a lock file with per-field drift detection (a changed *description* is the tool-poisoning shape), exit codes for CI, 41 tests, stdlib only, no network or SDK. **Silent on a well-behaved server — that is a test**, because my first version flagged a read-only documentation search as a sink. Now sold as its own fixed-price engagement on the hire page. |
| **The three tools were separate stories** | `mcpaudit policy` now emits the policy that `policygate` enforces, from the same declarations: collisions and instruction-carrying descriptions deny, ambiguous tools escalate, clean read-only tools are allowed, and anything the audit never saw escalates. The integration is verified in CI by loading the generated policy with the real loader (policygate pinned to a commit, 71 tests on three Python versions). **Writing it found a supply-chain bug in the generator**: an unescaped tool name could inject its own `effect = "allow"` rule into the policy governing the server — four regression tests now cover it. |
| **The detectors asserted instead of measuring** | `tool-boundary-corpus` — 23 labelled cases (14 tool-list, 5 code, 4 declaration-drift) and a detector-agnostic harness. Measured: mcpaudit **P=1.000 R=1.000**, agentbound **P=0.750 R=1.000**. The corpus immediately found a real false positive in agentbound (a pattern inside a string literal, which its comment fix does not cover) — kept failing and named in CI rather than deleted. Two of my fixtures were wrong, not the detector: the rules require the framework to *define* the tool, and the full signature→params→filter relation. **Fixed and re-measured:** agentbound v0.1.10 extended its prose classifier to strings bound to a name; the corpus now measures it at precision 1.000 and the CI gate was tightened from a recall floor to precision and recall. |
| **Nobody could find any of it** | GitHub **topics** set on eight repositories, so they now surface in topic search (verified: `agent-review-sample` appears under `topic:agent-security`). Draft posts written for Hacker News, r/LocalLLaMA, Show HN, r/netsec and LinkedIn in `job-kit/LAUNCH-POSTS.md` — **posting them needs your accounts**, one per day. |
| **Only one piece of writing** | A second writeup, and the more important one: [*Structured output made my safety evaluator less safe*](https://github.com/sushant-me/writeups/blob/main/2026-09-20-structured-output-made-it-less-safe.md) — the paper's counter-intuitive result, with all 21 numbers checked against the paper's own README before publication. Now the lead item in the profile's writing section. |
| **The benchmark measured whatever detector was on `PATH`** | Pinning *which build* a score came from turned up three defects, all the same shape — a number nothing checked. (1) A stale `agentbound` 0.1.8 on `PATH` reported precision **0.750**, the exact number the corpus exists to prove was fixed; the bug and the regression were indistinguishable because nothing asked which detector it was measuring. Both rows now identify the build first — agentbound by reported version, mcpaudit by the commit `pip` installed from, since it is pinned to a commit and its version string is not bumped per commit — and CI sets `CORPUS_REQUIRE_DETECTORS=1` so a mismatch fails there instead of skipping. (2) Writing that check meant reading `agentbound --version`, which printed **0.1.9** on the **v0.1.10** release: `pyproject.toml` was bumped and `agentbound/__init__.py` was not. [v0.1.11](https://github.com/sushant-me/agentbound/releases/tag/v0.1.11) carries the fix, guarded by a test comparing the two strings (verified by mutation). (3) Two tests imported `scripts.recall_audit` by module name, which resolves only when the repo root is on `sys.path` — green under `python -m pytest`, **red under a bare `pytest`** for the life of the file. They load by path now and CI runs both invocations. Also corrected two numbers nothing had checked: agentbound's test count is **111 → 114** (published as 114 → 117) and the corpus README's own suite is thirty-two tests (published as 17). |
| **The benchmark's own story had no post** | [*A benchmark found a bug in my own detector*](https://github.com/sushant-me/writeups/blob/main/2026-09-21-a-benchmark-found-a-bug-in-my-own-detector.md) — precision 0.750 → 1.000, the two fixtures that were wrong before the detector was, the harness that assumed its author's output format, and the three defects above. Every number re-measured from source before publishing: the `25 → 16` repo-root scan was reproduced by building a worktree at each fix commit and running that commit's own engine over its own tree. |
| **A load-bearing claim had gone stale on eight surfaces** | The most-cited receipt described the go-github patch as merged, and named #4556's per-method host check as the thing in the library. That check **is not in master**: the maintainer reverted it the next day in favour of the broader #4564, which enforces *"credentials are sent only to configured origins"*. Anyone who clicked through would have found the code gone. Corrected on the profile, the hire page, both CVs, both PDFs, the evidence sheet, the portfolio source and the LinkedIn draft — and the corrected version is **stronger**: my fix merged, the maintainer generalised it, and the merged replacement carries the commit *"Address feedback from sushant-me"*. It also surfaced that some pages still called two **closed** pull requests "issues under review" (#675, #680), and that #675 was described as *"under review by a Google maintainer"* long after it was closed. |
| **The claim was a sentence nothing re-derived** | `verify_evidence.py` now checks the go-github claim's four facts, including the **negative** one: that the promised co-author credit has *not* landed. A claim that something is still owed is the kind that rots silently, so the day the trailer appears the verifier goes red and the claim gets rewritten. Tested in all three directions by mutation: a missing marker in master, a paraphrase presented as a quote, and a simulated credit landing each fail the check with the message saying which. Writing it also caught a real bug in the check — the REST API returns `user`, not `author`. |
| **A figure I had published on nine surfaces was wrong** | The claim attributed a 16 GiB allocation to a **five-byte** header. Issue [#677](https://github.com/google/s2geometry/issues/677) says the input is **117 bytes** — and no issue in the repository mentions five bytes anywhere. It had spread to the profile, the hire page, the portfolio, the LinkedIn draft, both attachable PDFs and the evidence file, and nothing had ever compared the number to the text it described. Corrected everywhere, and now **checked**: `verify_evidence.py` asserts that the figures each claim publishes actually appear in the issue that is their source (5 of them, across #676/#677/#679). Mutation-tested by re-introducing the exact original error — a message naming the issue and the size the claim publishes that the issue does not state. The correction makes the claim *better*: 117 bytes is still absurdly small. |
| **Nothing prepared you for the hostile interview** | `job-kit/INTERVIEW-STRESS.md` — the nine questions a sharp interviewer asks *after* opening your links, each with what it is really testing, the honest answer, the trap to avoid, and the public evidence for it. It includes the two questions that are uncomfortable because they are true: that the corpus is self-authored (so the 1.000 is a regression gate, not a benchmark), and that a fabricated quotation once reached the live profile — answered with the mechanism that caught it rather than a promise to be careful. Plus a four-step shape for a question you cannot answer, and the five numbers to have at your fingertips. |
| **Three errors in a row were found by hand, one round at a time** | That is a process failure, not three unlucky mistakes: each was a detail *inside* a claim that had been true when written, which a claim-level verifier cannot see. `check_surfaces.py` now enforces the error history itself — every wrong string is **forbidden** on every declared surface with the reason it is wrong, every corrected string is **required** so a fix-by-deletion fails, and every `releases/tag/vX.Y.Z` must equal the repository's actual latest release (scoped to the surfaces where a version link means "current" — the corpus README links v0.1.10 as the release that *fixed* a false positive, which is history). Mutation-tested in all three directions before it was trusted: re-injecting the size error fails, deleting a corrected phrase fails, and a link one version back fails. **Coverage is asserted** (`--min-surfaces`, and it prints declarations matched out of declared) because a checker that found no files would be the same bug with a green tick — which is exactly what CI did on its first run, when this repo was checked out at the root and only one surface matched. The six project READMEs were added next, and auditing them found a fourth error: policygate called the paper's 90.8% "decisive rules" when the paper's label is "Rule A hard denials". |
| **The page limit kept the best material out of the paper, and the portal wanted it** | The supplementary field was empty because nothing assembled the document — `docs/FINDINGS.md`, `docs/DATA_SCHEMA.md` and `results/tables.md` were all written and none of them was a file to upload. `docs/build_supplement.py` now builds one, and two properties make it checkable rather than merely formatted: the reproducibility block is **the repository's own checker executed at build time and embedded verbatim**, and the corpus plus four results files are **hashed into an inventory**, so a reader can confirm the supplement describes the bytes they have. `make supplement` runs it. **Building it found a real bug**: three of the five tables in `results/tables.md` had a delimiter row one cell short, because the delimiter was hand-counted while the header had an extra column. GitHub renders that anyway, so it survived; a strict Markdown parser drops the table, which means every conversion of that artifact was silently losing rows. The delimiter now derives from the header, and `make verify` gained a fourth check — which was itself wrong first, treating an empty line as a delimiter row. |
| **A single score would have hidden both mechanisms** | Ran the third-party scanner over all 14 tool-list cases with `adapters/compare.py`, which reports **which classes** each detector covers instead of one averaged number: mcpaudit 9/9 with no false positive on the five negatives, `mcp-security-scanner` 0/9. Seven of those nine misses are classes it never claimed — and **two are classes where it does**, which is the whole reason not to publish a headline number. **Instruction in a description:** both are keyword lists. The corpus fixture says "Ignore all previous instructions" and its check passes; swap in "ignore any safety rules" — its own phrase, same tool, same schema — and it fails. Each catches what the other misses, and neither generalises, which is a statement about this whole class of tool including mine. **Dangerous capability:** name-driven, as round 26 measured. Comparing against a tool solving a *different* problem also showed what my corpus lacked: it has checks for description stability over time, prompt templates, resources, auth and transport, and the corpus had no case for any of them. **The first of those is now closed** — four `tool-list-drift` cases carry two states of the same server, because a rug pull cannot be expressed as one payload. Prompt templates, resources, auth and transport remain named gaps — named as a gap rather than left to be discovered. |
| **The promised conformance table had no rows** | Round 25 proved the best-known scanner analyses server-side. This round found one that runs fully offline — **`mcp-security-scanner` 0.1.5**, the `mcp-scan` CLI — and produced the first real third-party measurement. Two adapters went into the corpus: `adapters/serve_tools.py`, because that scanner reads declarations by **launching a server**, and `adapters/mcp_scanner_adapter.py`, which maps only the two checks that overlap and says so. The measured difference is worth the whole exercise: on the one class where both claim coverage, the fixture's tool passes its dangerous-capability check as `run_task` and **fails it as `run_shell`, with the schema unchanged**. It reads the danger from the tool's *name*; this corpus's rule reads the *parameter*, which is where the capability is. Recorded without a precision/recall row, because a single number would measure the mismatch between two scopes rather than the quality of either. |
| **My own checker could be evaded by a line break** | Two required strings failed on documents that plainly contained them: Markdown had wrapped the phrase across two lines and the matcher compared single-space literals. The same hole meant a **forbidden** string split across a line would have gone unreported — a rule evadable by reformatting. Matching now runs on whitespace-normalised text, verified by injecting the input-size error with a line break inside it and watching it fail. Found because a rule I wrote failed for the wrong reason. |
| **The conformance deliverable promised something that cannot be done offline** | The proposal's D3 said "run every available agent-framework scanner". Trying the first one: **Snyk Agent Scan v0.6.3** (the package formerly published as `mcp-scan`) inspects declarations on the machine correctly and then calls a **hosted endpoint** for the verdict — `api.snyk.io/hidden/mcp-scan/analysis`, from `verify_api.py`; offline it exits 1 with **nothing on stdout**, which this harness would score as an error on every case rather than a number. So it cannot be measured here without an account, a network path, and sending the corpus to a third party. Recorded in the corpus README with the trade written out (local: reproducible offline, declarations stay put, frozen until you upgrade — hosted: the reverse), and D3 now promises every scanner that *can* be run and names the ones that cannot. That difference is also a property to sell rather than hide: the audit service is a **local static check**, so a client's declarations never leave their machine. |
| **A service was sold with no deliverable to look at** | The hire page sells an MCP server audit whose output nobody could see — the other three services each had an artifact (a review sample, memory-safety reproducers, a labelled corpus) and this one had only the tool. `sushant-me/mcp-audit-sample` is a deliberately risky vendor server **and the written audit I would return for it**: ten findings, two clean tools, and the shapes that matter in this class — a name that displaces a framework primitive, an instruction hidden in the Unicode tag block, a description telling the model to add credentials to its summary, a destructive tool declaring `readOnlyHint`, a look-alike pair. What makes it a deliverable rather than a document: `demo.py` **exits non-zero when a finding stops reproducing**, the audit emits the policy the gate enforces, and a lock file turns the tool-poisoning shape into a reported diff (`--drift` demonstrates it). Building it also forced a real gap closed — **mcpaudit had no tag or release at all**, so its README version described nothing fetchable; v0.1.1 is now tagged, which is also the pin the sample's CI installs. |
| **A linter that fired on documentation** | Found by auditing a realistic vendor server rather than mcpaudit's own example: the mutation hint list was matched as a **substring**, so `get_runbook` matched "run", `list_postgres_instances` matched "post" and `get_updates` matched "update". Ten of twelve plausible read-only names I tried, each producing a **HIGH** finding on a server whose tools only read — the exact failure the module's own docstring warns about, which it had already been fixed for once from the other direction. Fixed in **v0.1.1**: match whole tokens, drop the generic verb "run" (the dangerous `run_command` shape is caught by the sink check), and treat a name opening with `get_`/`list_`/`read_` as a reader unless its description contradicts it. The corpus case `tools-readonly-names-with-mutation-words` holds the fix, and it **caught a flaw in the first version of that fix** — the getter rule ignored that a description restates the noun, so `get_grant_balance` still matched on "grant". Re-measured 14/14 at precision 1.000, recall 1.000. |
| **The corpus count lived in eight places** | Growing the corpus to 19 cases made "18 cases" wrong on the profile, the hire page, the proposal (twice, in the Markdown and the HTML), the ledger, two evidence fields, a **queued email** and a launch draft — and the tool-list subset moved 13 to 14 in a published post. All corrected, and both shapes are now pinned: five forbidden spellings of the old count, eight required spellings of the new one, and `job-kit/outbox.json` added as a scanned surface, because a queued email is text that would have been sent. **Then it happened again, one move later**, which is the point of writing the rule down: adding the declaration-drift class took the corpus from nineteen cases to 23, and the same eight surfaces were wrong again. Both moves are now pinned — the nineteen-case spellings are forbidden alongside the eighteen-case ones, and the required spellings moved to 23. The suite count moved too (thirty-two to 54) and is checked the same way, because a number in prose with no rule behind it is the thing that goes stale. **One surface the count does not reach is the corpus README's own code fences**: an earlier edit there had left an empty reproduce block, which rendered a `###` section heading as literal code, and a fence marker glued onto the end of a sentence. Nothing rendered it, so nothing caught it; the corpus suite now asserts its README's fences are balanced and enclose only code. **The deployed pages were the last place a count could rot**, and nothing was looking at them: `LIVE_SITES` asserted other phrases and never a number. `check_surfaces.py` now runs the same derivation over the fetched bytes, so a page that says the old count fails with *the deployment is behind the repository* rather than passing. **One more thing this move taught, by failing:** the checks span four repositories, and pushing them in the wrong order makes the run red for a reason that is not a defect — the next run at the same commit is green once the sibling pushes land. A red run here therefore means *check the siblings*, not *the claim is wrong*. |
| **The strongest claim was the least checkable** | The paper's numbers are the most valuable thing on the record and the one a reader can least verify by clicking. The repository ships its corpus generator, its raw model outputs for every condition and its own checker — so `verify_evidence.py` now **clones it and runs that checker**, weekly. It passes: the 600-scenario corpus regenerates byte-for-byte, `metrics.json` reproduces from the raw outputs across 5 conditions, and 100 macros derive from it with recorded constants declared. No model execution, ~2 s and 9 MB, which is why it can run rather than be taken on trust. Every number quoted elsewhere was re-derived from those artifacts while adding it: unsafe accepts **17.2% / 46.2% / 23.5%**, decision accuracy **66.3%**, **6 of 208** hard denials in the best arm — where 208 is the count of Rule A scenarios in the committed corpus, not a number from memory. |
| **A count written into six sentences** | Adding that claim made the claim/live counts wrong in six places across four files — and they already disagreed with each other, one still saying 15 from two rounds earlier. Fifth instance of the same class, so the fix is structural: `check_surfaces.py` now **computes** the counts from `evidence.json` and `verify_evidence.CHECKS` and fails any sentence that differs. Eleven counts are checked on the workstation (five in CI, where job-kit is absent), mutation-tested by changing one to a wrong value. Scanning all of `reputation/*.md` for the first time also caught the ledger quoting two of the errors it documents; both now describe them instead of reproducing them, because an exemption for the documentation is the escape hatch that let these errors through. |
| **"Contract work if there is any" had no verified route** | Two were found and read off their own pages, and both are worth more than a generic application because their *process* rewards public work samples over credentials. **METR**: *"The core of our hiring process is our work tests… We provide work test honoraria… whether or not the candidate moves forward"*, weighted **above interviews** — the best-fitting selection format anywhere — but every open role is Berkeley on-site, so the action is an honest question email (queued) rather than an application. **Trail of Bits**: an open *talent pipeline* for *"If your background is unusual or strong, send it our way and we will route you to the right team"*, where *"a strong work sample you already have can stand in"* for the take-home — and `agent-review-sample` is exactly that. Honest caveat recorded for both: ToB's live roles are US/UK, so it is a pipeline submission, not a posting. Details and the exact quotes are in `job-kit/LIVE-OPENINGS.md`; both are rows in `job-kit/APPLICATION-TRACKER.md`. |
| **The artifact that reaches the employer was the one nothing checked** | `check_surfaces.py` has understood PDFs since it was written - `read()` calls `pdftotext` for a `.pdf` - and **no `SURFACES` pattern listed one**. The CVs and the fund proposal are attached to applications, and they were the only public-facing documents no rule had ever been run against. Adding `cv/*.pdf`, `reputation/*.pdf` and `job-kit/*.pdf` found a live error immediately: **`PROPOSAL.pdf` still carried the previous corpus count** - the nineteen-case figure, in two sentences - after the corpus had grown to 23 and the number had been corrected in `PROPOSAL.md` and `PROPOSAL.html`. The PDF is generated from the HTML by hand, so the correction never reached the copy a funder would open. Regenerated (2 pages, A4, unchanged in length), and the surface count went 22 to 25 declarations and 47 to 53 files. The same pass found that the AI-security CV listed three released tools and omitted `mcpaudit` (v0.1.1, 74 tests) and `tool-boundary-corpus` (23 labelled cases) - the two most substantial recent artifacts - so both are on it now. **The first version of the coverage guard was dead code**: it hard-coded three directories that already had `*.pdf` globs, so it could never fail. It derives its directories from `SURFACES` now, and a stray PDF placed in `writeups/` is caught. |
| **A credit that was owed, offered, and landed** | The go-github `Co-authored-by` trailer never appeared after #4564 replaced my #4556. The maintainer wrote on 2026-09-20 instead offering a mechanism that suits the repository's squash-merge policy: the contributor opens a PR adding themselves to `AUTHORS` and records the work in its description. I opened [google/go-github#4579](https://github.com/google/go-github/pull/4579) and **he merged it the same day** (17:11:32Z, merge commit `5493527`) — `Sushant Poudel <sushant.poudel2028@gmail.com>` is in [AUTHORS](https://github.com/google/go-github/blob/master/AUTHORS) on master. The lesson worth keeping: the answer to "my credit is missing" was not to ask again but to take the route the maintainer proposed, and the verifier — which asserted only the negative — now checks the positive as well, because it would otherwise have gone on reporting a landed credit as outstanding. |

## Waiting on other people (your next action is a reminder, not work)

| loop | state | next action | when |
|---|---|---|---|
| s2geometry **#681**, **#682** | open, all checks pass; the *new* CI runs need a Google maintainer to approve them | if nothing moves, one short polite comment on #682 | 7 days |
| adk-go **#1606**, adk-java **#1515**, adk-python **#7145** | open, awaiting review; the red checks are fork-PR gating, not code | leave them; reply only if a maintainer replies | — |
| libphonenumber **#4079**, gson **#3121** | open, no reviewer yet | leave them | — |
| go-github **#4556** → **#4564** | merged, then superseded; **#4564 is in master** and carries the commit *"Address feedback from sushant-me"* | **Commented on #4366 on 2026-09-20** ([comment](https://github.com/google/go-github/issues/4366#issuecomment-5747072339)) asking once for the `Co-authored-by` trailer he offered, with an easy out. Nothing further to do — do **not** follow up again. If the trailer lands, `verify_evidence.py` goes red on purpose and the claim gets upgraded | waiting; no action |

## Yours to do, with a date

| loop | why it matters | next action |
|---|---|---|
| **Firewall paper — camera-ready + supplementary file** | the portal status says *"Accept with Revision"* and **"Supplementary File Not Uploaded"**. "One upload away" was hiding an undefined task: **no supplementary file existed**, so there was nothing to upload. It does now. | **Upload `docs/supplementary.pdf`** (9 pages, at `Edge-Native_Semantic_Firewall_/docs/supplementary.pdf` locally, or [on GitHub](https://github.com/sushant-me/Edge-Native_Semantic_Firewall_/blob/main/docs/supplementary.pdf)). It is generated by `make supplement` from the committed artifacts: extended findings, data schema, full result tables, an artifact inventory with sha256s, and a reproducibility block that is the repository's own checker run at build time — so it cannot describe a different revision than the one it was built from. Then confirm the camera-ready deadline. | **now** |
| **PREBAS** | accepted; a paper that is accepted but not presented is an unfinished credential | confirm registration, presentation format and travel/remote option | 
| **CV placeholders** | the university CV has two blanks I refused to invent | fill in the expected graduation month and the high-school GPA | 
| **Sending the applications at all** | you gave permission to email, and there is **no credential to send with** — the machine has no mail client, no SMTP host/user/password, no mail API key, and `gh`/Telegram do not send email. The sender is built and **18 tests cover it, including one that sends the real outbox end-to-end** through the code path: `job-kit/mailer.py` is dry-run by default, refuses any address not marked `verified` (with the source URL it was read from), never prints the password, and will not email the same person twice (`SENT.log`). **Three messages are now queued**, each citing where its address came from: the Transformative AI Fund managers, the OpenAI fellowship office (next cycle), and METR | create a **Gmail app password** (Google Account → Security → 2-Step Verification → App passwords) and write `~/.config/job-kit/smtp` with `host=smtp.gmail.com`, `user=sushant.poudel2028@gmail.com`, `password=<the 16-character app password>`. Then `python3 job-kit/mailer.py --send` sends all three; `python3 job-kit/mailer.py` alone shows them without sending | the single highest-payoff unblock |
| **Fellowship timing** | researched and verified: the OpenAI Safety Fellowship **closed 3 May** (its cohort started 14 Sept), MATS **Winter 2027 is closed**, and the Long-Term Future Fund no longer exists. The rolling route that is open today is the **Transformative AI Fund** (individuals, any country, $10k–$150k) | read `job-kit/FELLOWSHIPS.md`; the fund enquiry is queued in the outbox, and the fellowship scope question is queued with it. **The proposal those applications need now exists**: `job-kit/PROPOSAL.md` (+ `PROPOSAL.pdf`, 2 pages) — *Fail-closed enforcement for agent tool calls*, with a research question, three deliverables each carrying an acceptance test that can fail, a $48k/12-month budget, and three stated ways the bet could be wrong. The next window is **early 2027**; what remains is tightening it, not writing it | now, then Jan 2027 |
| **Transformative AI Fund draft** | a full draft exists and is eligible (individuals, any country) | an enquiry email is queued in `job-kit/outbox.json` and sends once the credential exists; then submit the full draft, or shelve it deliberately — do not leave it in limbo | 
| ~~Live site shows the old go-github wording~~ | **This loop was wrong, and wrong for several rounds.** I reasoned that because `sushantpoudel2028.com.np` is not served from the `Portfolio` repo by GitHub Pages, correcting `page.tsx` could not have changed what a visitor reads. It had: the host is a Cloudflare-fronted static export (`next.config.ts` sets `output: 'export'`) that rebuilds on every push to `main`, and the live page has carried the corrections since the commit that made them — `117-byte` present, the superseded size absent, `generalised into` present. **Nothing to do.** `check_surfaces.py` now fetches both deployed pages and asserts this, so "committed but not deployed" is a failure rather than a guess — and so is instructing you to redeploy a site that has already rebuilt itself. | done |
| **`tdd-bdd-final-project` archive state** | I unarchived it to fix CI; that was a change to your account you did not ask for | decide: keep it active (`gh api -X PATCH ... -f archived=true` reverts) | 
| **HackingHub Q3 #1** | the board resets each quarter, so this becomes a dated achievement rather than a current one | decide whether to defend it in Q4 (costs time) or let it stand | 
| **WATCHLIST CTF — 4 labs** | low value now that the event context has passed | park it explicitly | 

## The standing decision

**One spine: AI agent security and verification.** The papers, the Google pull requests,
`agentbound` / `mcp-nameguard` / `trajectorycheck`, and the memory-safety work all already point at it.
The rest — CTF rank, Hult Prize, the Flutter apps, the 100+ exercise repositories — is supporting
evidence, not a competing headline.

**And the rule for the next month: start nothing new.** Every loop above is either someone else's
turn or a small finishing task. The work that creates reputation now is finishing, publishing and
applying — not adding a fourteenth project.

## How to keep this list honest

Re-run it weekly and delete rows that are done:

```bash
cd reputation && python3 verify_evidence.py     # 0 failing = your public claims are still true
gh api notifications --paginate -q 'length'     # 0 = nothing unread waiting on you
```

If a row has no next action, it is not a loop — it is a decision you have not made yet. Make it.
